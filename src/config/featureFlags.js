/**
 * Product feature toggles (client-only). Keep related UI and API wiring in place;
 * flip to true when re-launching the capability.
 */

/** AI Listening Assessment: create-exam module, voice preview, and in-exam audio player. */
export const FEATURE_AI_LISTENING = false;
